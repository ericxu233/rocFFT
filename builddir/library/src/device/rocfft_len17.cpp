/*
 * Stockham kernels generated by:
 * 
 *     /home/ugrad/ugrad003/roctosycl/rocFFT/library/src/device/kernel-generator.py --pattern=all --precision=all --groups=150 --manual-small= --manual-large= generate /home/ugrad/ugrad003/roctosycl/rocFFT/builddir/library/src/device/generator/rocfft-kernel-generator
 * 
 * Generator is: /home/ugrad/ugrad003/roctosycl/rocFFT/library/src/device/kernel-generator.py
 * Kernel is: namespace(factors=[17], flavour='uwide', length=17, scheme='CS_KERNEL_STOCKHAM', threads_per_block=256)
 */

#include "kernel_launch.h"
#include "real2complex.h"
#include "rocfft_butterfly_template.h"
#include <hip/hip_runtime.h>

/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void ip_forward_length17_SBRR_device(scalar_type* __restrict__ lds,
                                                const scalar_type* __restrict__ twiddles,
                                                size_t       stride_lds,
                                                unsigned int offset_lds,
                                                bool         write)
{
    size_t       thread;
    scalar_type  R[17];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 1; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0]  = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1]  = lds[offset_lds + (thread + 1) * lstride]; /* stockham.py:542 */
    R[2]  = lds[offset_lds + (thread + 2) * lstride]; /* stockham.py:542 */
    R[3]  = lds[offset_lds + (thread + 3) * lstride]; /* stockham.py:542 */
    R[4]  = lds[offset_lds + (thread + 4) * lstride]; /* stockham.py:542 */
    R[5]  = lds[offset_lds + (thread + 5) * lstride]; /* stockham.py:542 */
    R[6]  = lds[offset_lds + (thread + 6) * lstride]; /* stockham.py:542 */
    R[7]  = lds[offset_lds + (thread + 7) * lstride]; /* stockham.py:542 */
    R[8]  = lds[offset_lds + (thread + 8) * lstride]; /* stockham.py:542 */
    R[9]  = lds[offset_lds + (thread + 9) * lstride]; /* stockham.py:542 */
    R[10] = lds[offset_lds + (thread + 10) * lstride]; /* stockham.py:542 */
    R[11] = lds[offset_lds + (thread + 11) * lstride]; /* stockham.py:542 */
    R[12] = lds[offset_lds + (thread + 12) * lstride]; /* stockham.py:542 */
    R[13] = lds[offset_lds + (thread + 13) * lstride]; /* stockham.py:542 */
    R[14] = lds[offset_lds + (thread + 14) * lstride]; /* stockham.py:542 */
    R[15] = lds[offset_lds + (thread + 15) * lstride]; /* stockham.py:542 */
    R[16] = lds[offset_lds + (thread + 16) * lstride]; /* stockham.py:542 */

    FwdRad17B1(&R[0],
               &R[1],
               &R[2],
               &R[3],
               &R[4],
               &R[5],
               &R[6],
               &R[7],
               &R[8],
               &R[9],
               &R[10],
               &R[11],
               &R[12],
               &R[13],
               &R[14],
               &R[15],
               &R[16]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 8) * lstride]
            = R[8]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 9) * lstride]
            = R[9]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 10) * lstride]
            = R[10]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 11) * lstride]
            = R[11]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 12) * lstride]
            = R[12]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 13) * lstride]
            = R[13]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 14) * lstride]
            = R[14]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 15) * lstride]
            = R[15]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 16) * lstride]
            = R[16]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(120) void ip_forward_length17_SBRR(const scalar_type* __restrict__ twiddles,
                                                         const size_t dim,
                                                         const size_t* __restrict__ lengths,
                                                         const size_t* __restrict__ stride,
                                                         const size_t       nbatch,
                                                         const unsigned int lds_padding,
                                                         void* __restrict__ load_cb_fn,
                                                         void* __restrict__ load_cb_data,
                                                         uint32_t load_cb_lds_bytes,
                                                         void* __restrict__ store_cb_fn,
                                                         void* __restrict__ store_cb_data,
                                                         scalar_type* __restrict__ buf)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf, offset + (thread + 0) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf, offset + (thread + 1) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf, offset + (thread + 2) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf, offset + (thread + 3) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf, offset + (thread + 4) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf, offset + (thread + 5) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf, offset + (thread + 6) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf, offset + (thread + 7) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf, offset + (thread + 8) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf, offset + (thread + 9) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf, offset + (thread + 10) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf, offset + (thread + 11) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf, offset + (thread + 12) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf, offset + (thread + 13) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf, offset + (thread + 14) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf, offset + (thread + 15) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf, offset + (thread + 16) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = load_cb(buf, offset + (thread + 16 + 1) * stride0, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(
        buf, offset + (thread + 0) * stride0, lds[offset_lds + thread + 0], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 1) * stride0, lds[offset_lds + thread + 1], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 2) * stride0, lds[offset_lds + thread + 2], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 3) * stride0, lds[offset_lds + thread + 3], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 4) * stride0, lds[offset_lds + thread + 4], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 5) * stride0, lds[offset_lds + thread + 5], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 6) * stride0, lds[offset_lds + thread + 6], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 7) * stride0, lds[offset_lds + thread + 7], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 8) * stride0, lds[offset_lds + thread + 8], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 9) * stride0, lds[offset_lds + thread + 9], store_cb_data, nullptr);
    store_cb(buf,
             offset + (thread + 10) * stride0,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 11) * stride0,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 12) * stride0,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 13) * stride0,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 14) * stride0,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 15) * stride0,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 16) * stride0,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf,
                     offset + (thread + 16 + 1) * stride0,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void ip_forward_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ bufre,
    real_type_t<scalar_type>* __restrict__ bufim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread                       = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0] = {bufre[offset + (thread + 0) * stride0],
                                    bufim[offset + (thread + 0) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 1] = {bufre[offset + (thread + 1) * stride0],
                                    bufim[offset + (thread + 1) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 2] = {bufre[offset + (thread + 2) * stride0],
                                    bufim[offset + (thread + 2) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 3] = {bufre[offset + (thread + 3) * stride0],
                                    bufim[offset + (thread + 3) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 4] = {bufre[offset + (thread + 4) * stride0],
                                    bufim[offset + (thread + 4) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 5] = {bufre[offset + (thread + 5) * stride0],
                                    bufim[offset + (thread + 5) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 6] = {bufre[offset + (thread + 6) * stride0],
                                    bufim[offset + (thread + 6) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 7] = {bufre[offset + (thread + 7) * stride0],
                                    bufim[offset + (thread + 7) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 8] = {bufre[offset + (thread + 8) * stride0],
                                    bufim[offset + (thread + 8) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 9] = {bufre[offset + (thread + 9) * stride0],
                                    bufim[offset + (thread + 9) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {bufre[offset + (thread + 10) * stride0],
           bufim[offset + (thread + 10) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {bufre[offset + (thread + 11) * stride0],
           bufim[offset + (thread + 11) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {bufre[offset + (thread + 12) * stride0],
           bufim[offset + (thread + 12) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {bufre[offset + (thread + 13) * stride0],
           bufim[offset + (thread + 13) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {bufre[offset + (thread + 14) * stride0],
           bufim[offset + (thread + 14) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {bufre[offset + (thread + 15) * stride0],
           bufim[offset + (thread + 15) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {bufre[offset + (thread + 16) * stride0],
           bufim[offset + (thread + 16) * stride0]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {bufre[offset + (thread + 16 + 1) * stride0],
                   bufim[offset + (thread + 16 + 1) * stride0]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    bufre[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    bufim[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    bufre[offset + (thread + 1) * stride0] = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    bufim[offset + (thread + 1) * stride0] = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    bufre[offset + (thread + 2) * stride0] = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    bufim[offset + (thread + 2) * stride0] = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    bufre[offset + (thread + 3) * stride0] = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    bufim[offset + (thread + 3) * stride0] = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    bufre[offset + (thread + 4) * stride0] = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    bufim[offset + (thread + 4) * stride0] = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    bufre[offset + (thread + 5) * stride0] = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    bufim[offset + (thread + 5) * stride0] = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    bufre[offset + (thread + 6) * stride0] = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    bufim[offset + (thread + 6) * stride0] = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    bufre[offset + (thread + 7) * stride0] = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    bufim[offset + (thread + 7) * stride0] = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    bufre[offset + (thread + 8) * stride0] = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    bufim[offset + (thread + 8) * stride0] = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    bufre[offset + (thread + 9) * stride0] = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    bufim[offset + (thread + 9) * stride0] = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    bufre[offset + (thread + 10) * stride0]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    bufim[offset + (thread + 10) * stride0]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    bufre[offset + (thread + 11) * stride0]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    bufim[offset + (thread + 11) * stride0]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    bufre[offset + (thread + 12) * stride0]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    bufim[offset + (thread + 12) * stride0]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    bufre[offset + (thread + 13) * stride0]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    bufim[offset + (thread + 13) * stride0]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    bufre[offset + (thread + 14) * stride0]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    bufim[offset + (thread + 14) * stride0]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    bufre[offset + (thread + 15) * stride0]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    bufim[offset + (thread + 15) * stride0]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    bufre[offset + (thread + 16) * stride0]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    bufim[offset + (thread + 16) * stride0]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            bufre[offset + (thread + 16 + 1) * stride0]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            bufim[offset + (thread + 16 + 1) * stride0]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void op_forward_length17_SBRR_device(scalar_type* __restrict__ lds,
                                                const scalar_type* __restrict__ twiddles,
                                                size_t       stride_lds,
                                                unsigned int offset_lds,
                                                bool         write)
{
    size_t       thread;
    scalar_type  R[17];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 1; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0]  = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1]  = lds[offset_lds + (thread + 1) * lstride]; /* stockham.py:542 */
    R[2]  = lds[offset_lds + (thread + 2) * lstride]; /* stockham.py:542 */
    R[3]  = lds[offset_lds + (thread + 3) * lstride]; /* stockham.py:542 */
    R[4]  = lds[offset_lds + (thread + 4) * lstride]; /* stockham.py:542 */
    R[5]  = lds[offset_lds + (thread + 5) * lstride]; /* stockham.py:542 */
    R[6]  = lds[offset_lds + (thread + 6) * lstride]; /* stockham.py:542 */
    R[7]  = lds[offset_lds + (thread + 7) * lstride]; /* stockham.py:542 */
    R[8]  = lds[offset_lds + (thread + 8) * lstride]; /* stockham.py:542 */
    R[9]  = lds[offset_lds + (thread + 9) * lstride]; /* stockham.py:542 */
    R[10] = lds[offset_lds + (thread + 10) * lstride]; /* stockham.py:542 */
    R[11] = lds[offset_lds + (thread + 11) * lstride]; /* stockham.py:542 */
    R[12] = lds[offset_lds + (thread + 12) * lstride]; /* stockham.py:542 */
    R[13] = lds[offset_lds + (thread + 13) * lstride]; /* stockham.py:542 */
    R[14] = lds[offset_lds + (thread + 14) * lstride]; /* stockham.py:542 */
    R[15] = lds[offset_lds + (thread + 15) * lstride]; /* stockham.py:542 */
    R[16] = lds[offset_lds + (thread + 16) * lstride]; /* stockham.py:542 */

    FwdRad17B1(&R[0],
               &R[1],
               &R[2],
               &R[3],
               &R[4],
               &R[5],
               &R[6],
               &R[7],
               &R[8],
               &R[9],
               &R[10],
               &R[11],
               &R[12],
               &R[13],
               &R[14],
               &R[15],
               &R[16]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 8) * lstride]
            = R[8]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 9) * lstride]
            = R[9]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 10) * lstride]
            = R[10]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 11) * lstride]
            = R[11]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 12) * lstride]
            = R[12]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 13) * lstride]
            = R[13]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 14) * lstride]
            = R[14]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 15) * lstride]
            = R[15]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 16) * lstride]
            = R[16]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(120) void op_forward_length17_SBRR(const scalar_type* __restrict__ twiddles,
                                                         const size_t dim,
                                                         const size_t* __restrict__ lengths,
                                                         const size_t* __restrict__ stride_in,
                                                         const size_t* __restrict__ stride_out,
                                                         const size_t       nbatch,
                                                         const unsigned int lds_padding,
                                                         void* __restrict__ load_cb_fn,
                                                         void* __restrict__ load_cb_data,
                                                         uint32_t load_cb_lds_bytes,
                                                         void* __restrict__ store_cb_fn,
                                                         void* __restrict__ store_cb_data,
                                                         scalar_type* __restrict__ buf_in,
                                                         scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf_in, offset_in + (thread + 1) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf_in, offset_in + (thread + 2) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf_in, offset_in + (thread + 3) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf_in, offset_in + (thread + 4) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf_in, offset_in + (thread + 5) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf_in, offset_in + (thread + 6) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf_in, offset_in + (thread + 7) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf_in, offset_in + (thread + 8) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf_in, offset_in + (thread + 9) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf_in, offset_in + (thread + 10) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf_in, offset_in + (thread + 11) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf_in, offset_in + (thread + 12) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf_in, offset_in + (thread + 13) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf_in, offset_in + (thread + 14) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf_in, offset_in + (thread + 15) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf_in, offset_in + (thread + 16) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1] = load_cb(
                buf_in, offset_in + (thread + 16 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 1) * stride0_out,
             lds[offset_lds + thread + 1],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 2) * stride0_out,
             lds[offset_lds + thread + 2],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 3) * stride0_out,
             lds[offset_lds + thread + 3],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 4) * stride0_out,
             lds[offset_lds + thread + 4],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 5) * stride0_out,
             lds[offset_lds + thread + 5],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 6) * stride0_out,
             lds[offset_lds + thread + 6],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 7) * stride0_out,
             lds[offset_lds + thread + 7],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 8) * stride0_out,
             lds[offset_lds + thread + 8],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 9) * stride0_out,
             lds[offset_lds + thread + 9],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 10) * stride0_out,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 11) * stride0_out,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 12) * stride0_out,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 13) * stride0_out,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 14) * stride0_out,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 15) * stride0_out,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 16) * stride0_out,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf_out,
                     offset_out + (thread + 16 + 1) * stride0_out,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_forward_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    scalar_type* __restrict__ buf_in,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf_in, offset_in + (thread + 1) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf_in, offset_in + (thread + 2) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf_in, offset_in + (thread + 3) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf_in, offset_in + (thread + 4) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf_in, offset_in + (thread + 5) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf_in, offset_in + (thread + 6) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf_in, offset_in + (thread + 7) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf_in, offset_in + (thread + 8) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf_in, offset_in + (thread + 9) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf_in, offset_in + (thread + 10) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf_in, offset_in + (thread + 11) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf_in, offset_in + (thread + 12) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf_in, offset_in + (thread + 13) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf_in, offset_in + (thread + 14) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf_in, offset_in + (thread + 15) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf_in, offset_in + (thread + 16) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1] = load_cb(
                buf_in, offset_in + (thread + 16 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            buf_outre[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_forward_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 1]
        = {buf_inre[offset_in + (thread + 1) * stride0_in],
           buf_inim[offset_in + (thread + 1) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 2]
        = {buf_inre[offset_in + (thread + 2) * stride0_in],
           buf_inim[offset_in + (thread + 2) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 3]
        = {buf_inre[offset_in + (thread + 3) * stride0_in],
           buf_inim[offset_in + (thread + 3) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 4]
        = {buf_inre[offset_in + (thread + 4) * stride0_in],
           buf_inim[offset_in + (thread + 4) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 5]
        = {buf_inre[offset_in + (thread + 5) * stride0_in],
           buf_inim[offset_in + (thread + 5) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 6]
        = {buf_inre[offset_in + (thread + 6) * stride0_in],
           buf_inim[offset_in + (thread + 6) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 7]
        = {buf_inre[offset_in + (thread + 7) * stride0_in],
           buf_inim[offset_in + (thread + 7) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 8]
        = {buf_inre[offset_in + (thread + 8) * stride0_in],
           buf_inim[offset_in + (thread + 8) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 9]
        = {buf_inre[offset_in + (thread + 9) * stride0_in],
           buf_inim[offset_in + (thread + 9) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {buf_inre[offset_in + (thread + 10) * stride0_in],
           buf_inim[offset_in + (thread + 10) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {buf_inre[offset_in + (thread + 11) * stride0_in],
           buf_inim[offset_in + (thread + 11) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {buf_inre[offset_in + (thread + 12) * stride0_in],
           buf_inim[offset_in + (thread + 12) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {buf_inre[offset_in + (thread + 13) * stride0_in],
           buf_inim[offset_in + (thread + 13) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {buf_inre[offset_in + (thread + 14) * stride0_in],
           buf_inim[offset_in + (thread + 14) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {buf_inre[offset_in + (thread + 15) * stride0_in],
           buf_inim[offset_in + (thread + 15) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {buf_inre[offset_in + (thread + 16) * stride0_in],
           buf_inim[offset_in + (thread + 16) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {buf_inre[offset_in + (thread + 16 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 16 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 1) * stride0_out,
             lds[offset_lds + thread + 1],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 2) * stride0_out,
             lds[offset_lds + thread + 2],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 3) * stride0_out,
             lds[offset_lds + thread + 3],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 4) * stride0_out,
             lds[offset_lds + thread + 4],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 5) * stride0_out,
             lds[offset_lds + thread + 5],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 6) * stride0_out,
             lds[offset_lds + thread + 6],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 7) * stride0_out,
             lds[offset_lds + thread + 7],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 8) * stride0_out,
             lds[offset_lds + thread + 8],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 9) * stride0_out,
             lds[offset_lds + thread + 9],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 10) * stride0_out,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 11) * stride0_out,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 12) * stride0_out,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 13) * stride0_out,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 14) * stride0_out,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 15) * stride0_out,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 16) * stride0_out,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf_out,
                     offset_out + (thread + 16 + 1) * stride0_out,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_forward_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 1]
        = {buf_inre[offset_in + (thread + 1) * stride0_in],
           buf_inim[offset_in + (thread + 1) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 2]
        = {buf_inre[offset_in + (thread + 2) * stride0_in],
           buf_inim[offset_in + (thread + 2) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 3]
        = {buf_inre[offset_in + (thread + 3) * stride0_in],
           buf_inim[offset_in + (thread + 3) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 4]
        = {buf_inre[offset_in + (thread + 4) * stride0_in],
           buf_inim[offset_in + (thread + 4) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 5]
        = {buf_inre[offset_in + (thread + 5) * stride0_in],
           buf_inim[offset_in + (thread + 5) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 6]
        = {buf_inre[offset_in + (thread + 6) * stride0_in],
           buf_inim[offset_in + (thread + 6) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 7]
        = {buf_inre[offset_in + (thread + 7) * stride0_in],
           buf_inim[offset_in + (thread + 7) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 8]
        = {buf_inre[offset_in + (thread + 8) * stride0_in],
           buf_inim[offset_in + (thread + 8) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 9]
        = {buf_inre[offset_in + (thread + 9) * stride0_in],
           buf_inim[offset_in + (thread + 9) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {buf_inre[offset_in + (thread + 10) * stride0_in],
           buf_inim[offset_in + (thread + 10) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {buf_inre[offset_in + (thread + 11) * stride0_in],
           buf_inim[offset_in + (thread + 11) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {buf_inre[offset_in + (thread + 12) * stride0_in],
           buf_inim[offset_in + (thread + 12) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {buf_inre[offset_in + (thread + 13) * stride0_in],
           buf_inim[offset_in + (thread + 13) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {buf_inre[offset_in + (thread + 14) * stride0_in],
           buf_inim[offset_in + (thread + 14) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {buf_inre[offset_in + (thread + 15) * stride0_in],
           buf_inim[offset_in + (thread + 15) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {buf_inre[offset_in + (thread + 16) * stride0_in],
           buf_inim[offset_in + (thread + 16) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {buf_inre[offset_in + (thread + 16 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 16 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            buf_outre[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void ip_inverse_length17_SBRR_device(scalar_type* __restrict__ lds,
                                                const scalar_type* __restrict__ twiddles,
                                                size_t       stride_lds,
                                                unsigned int offset_lds,
                                                bool         write)
{
    size_t       thread;
    scalar_type  R[17];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 1; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0]  = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1]  = lds[offset_lds + (thread + 1) * lstride]; /* stockham.py:542 */
    R[2]  = lds[offset_lds + (thread + 2) * lstride]; /* stockham.py:542 */
    R[3]  = lds[offset_lds + (thread + 3) * lstride]; /* stockham.py:542 */
    R[4]  = lds[offset_lds + (thread + 4) * lstride]; /* stockham.py:542 */
    R[5]  = lds[offset_lds + (thread + 5) * lstride]; /* stockham.py:542 */
    R[6]  = lds[offset_lds + (thread + 6) * lstride]; /* stockham.py:542 */
    R[7]  = lds[offset_lds + (thread + 7) * lstride]; /* stockham.py:542 */
    R[8]  = lds[offset_lds + (thread + 8) * lstride]; /* stockham.py:542 */
    R[9]  = lds[offset_lds + (thread + 9) * lstride]; /* stockham.py:542 */
    R[10] = lds[offset_lds + (thread + 10) * lstride]; /* stockham.py:542 */
    R[11] = lds[offset_lds + (thread + 11) * lstride]; /* stockham.py:542 */
    R[12] = lds[offset_lds + (thread + 12) * lstride]; /* stockham.py:542 */
    R[13] = lds[offset_lds + (thread + 13) * lstride]; /* stockham.py:542 */
    R[14] = lds[offset_lds + (thread + 14) * lstride]; /* stockham.py:542 */
    R[15] = lds[offset_lds + (thread + 15) * lstride]; /* stockham.py:542 */
    R[16] = lds[offset_lds + (thread + 16) * lstride]; /* stockham.py:542 */

    InvRad17B1(&R[0],
               &R[1],
               &R[2],
               &R[3],
               &R[4],
               &R[5],
               &R[6],
               &R[7],
               &R[8],
               &R[9],
               &R[10],
               &R[11],
               &R[12],
               &R[13],
               &R[14],
               &R[15],
               &R[16]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 8) * lstride]
            = R[8]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 9) * lstride]
            = R[9]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 10) * lstride]
            = R[10]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 11) * lstride]
            = R[11]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 12) * lstride]
            = R[12]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 13) * lstride]
            = R[13]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 14) * lstride]
            = R[14]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 15) * lstride]
            = R[15]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 16) * lstride]
            = R[16]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(120) void ip_inverse_length17_SBRR(const scalar_type* __restrict__ twiddles,
                                                         const size_t dim,
                                                         const size_t* __restrict__ lengths,
                                                         const size_t* __restrict__ stride,
                                                         const size_t       nbatch,
                                                         const unsigned int lds_padding,
                                                         void* __restrict__ load_cb_fn,
                                                         void* __restrict__ load_cb_data,
                                                         uint32_t load_cb_lds_bytes,
                                                         void* __restrict__ store_cb_fn,
                                                         void* __restrict__ store_cb_data,
                                                         scalar_type* __restrict__ buf)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf, offset + (thread + 0) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf, offset + (thread + 1) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf, offset + (thread + 2) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf, offset + (thread + 3) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf, offset + (thread + 4) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf, offset + (thread + 5) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf, offset + (thread + 6) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf, offset + (thread + 7) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf, offset + (thread + 8) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf, offset + (thread + 9) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf, offset + (thread + 10) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf, offset + (thread + 11) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf, offset + (thread + 12) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf, offset + (thread + 13) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf, offset + (thread + 14) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf, offset + (thread + 15) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf, offset + (thread + 16) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = load_cb(buf, offset + (thread + 16 + 1) * stride0, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(
        buf, offset + (thread + 0) * stride0, lds[offset_lds + thread + 0], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 1) * stride0, lds[offset_lds + thread + 1], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 2) * stride0, lds[offset_lds + thread + 2], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 3) * stride0, lds[offset_lds + thread + 3], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 4) * stride0, lds[offset_lds + thread + 4], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 5) * stride0, lds[offset_lds + thread + 5], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 6) * stride0, lds[offset_lds + thread + 6], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 7) * stride0, lds[offset_lds + thread + 7], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 8) * stride0, lds[offset_lds + thread + 8], store_cb_data, nullptr);
    store_cb(
        buf, offset + (thread + 9) * stride0, lds[offset_lds + thread + 9], store_cb_data, nullptr);
    store_cb(buf,
             offset + (thread + 10) * stride0,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 11) * stride0,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 12) * stride0,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 13) * stride0,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 14) * stride0,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 15) * stride0,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 16) * stride0,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf,
                     offset + (thread + 16 + 1) * stride0,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void ip_inverse_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ bufre,
    real_type_t<scalar_type>* __restrict__ bufim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread                       = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0] = {bufre[offset + (thread + 0) * stride0],
                                    bufim[offset + (thread + 0) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 1] = {bufre[offset + (thread + 1) * stride0],
                                    bufim[offset + (thread + 1) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 2] = {bufre[offset + (thread + 2) * stride0],
                                    bufim[offset + (thread + 2) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 3] = {bufre[offset + (thread + 3) * stride0],
                                    bufim[offset + (thread + 3) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 4] = {bufre[offset + (thread + 4) * stride0],
                                    bufim[offset + (thread + 4) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 5] = {bufre[offset + (thread + 5) * stride0],
                                    bufim[offset + (thread + 5) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 6] = {bufre[offset + (thread + 6) * stride0],
                                    bufim[offset + (thread + 6) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 7] = {bufre[offset + (thread + 7) * stride0],
                                    bufim[offset + (thread + 7) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 8] = {bufre[offset + (thread + 8) * stride0],
                                    bufim[offset + (thread + 8) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 9] = {bufre[offset + (thread + 9) * stride0],
                                    bufim[offset + (thread + 9) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {bufre[offset + (thread + 10) * stride0],
           bufim[offset + (thread + 10) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {bufre[offset + (thread + 11) * stride0],
           bufim[offset + (thread + 11) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {bufre[offset + (thread + 12) * stride0],
           bufim[offset + (thread + 12) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {bufre[offset + (thread + 13) * stride0],
           bufim[offset + (thread + 13) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {bufre[offset + (thread + 14) * stride0],
           bufim[offset + (thread + 14) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {bufre[offset + (thread + 15) * stride0],
           bufim[offset + (thread + 15) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {bufre[offset + (thread + 16) * stride0],
           bufim[offset + (thread + 16) * stride0]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {bufre[offset + (thread + 16 + 1) * stride0],
                   bufim[offset + (thread + 16 + 1) * stride0]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    bufre[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    bufim[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    bufre[offset + (thread + 1) * stride0] = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    bufim[offset + (thread + 1) * stride0] = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    bufre[offset + (thread + 2) * stride0] = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    bufim[offset + (thread + 2) * stride0] = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    bufre[offset + (thread + 3) * stride0] = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    bufim[offset + (thread + 3) * stride0] = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    bufre[offset + (thread + 4) * stride0] = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    bufim[offset + (thread + 4) * stride0] = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    bufre[offset + (thread + 5) * stride0] = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    bufim[offset + (thread + 5) * stride0] = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    bufre[offset + (thread + 6) * stride0] = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    bufim[offset + (thread + 6) * stride0] = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    bufre[offset + (thread + 7) * stride0] = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    bufim[offset + (thread + 7) * stride0] = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    bufre[offset + (thread + 8) * stride0] = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    bufim[offset + (thread + 8) * stride0] = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    bufre[offset + (thread + 9) * stride0] = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    bufim[offset + (thread + 9) * stride0] = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    bufre[offset + (thread + 10) * stride0]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    bufim[offset + (thread + 10) * stride0]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    bufre[offset + (thread + 11) * stride0]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    bufim[offset + (thread + 11) * stride0]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    bufre[offset + (thread + 12) * stride0]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    bufim[offset + (thread + 12) * stride0]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    bufre[offset + (thread + 13) * stride0]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    bufim[offset + (thread + 13) * stride0]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    bufre[offset + (thread + 14) * stride0]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    bufim[offset + (thread + 14) * stride0]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    bufre[offset + (thread + 15) * stride0]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    bufim[offset + (thread + 15) * stride0]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    bufre[offset + (thread + 16) * stride0]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    bufim[offset + (thread + 16) * stride0]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            bufre[offset + (thread + 16 + 1) * stride0]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            bufim[offset + (thread + 16 + 1) * stride0]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void op_inverse_length17_SBRR_device(scalar_type* __restrict__ lds,
                                                const scalar_type* __restrict__ twiddles,
                                                size_t       stride_lds,
                                                unsigned int offset_lds,
                                                bool         write)
{
    size_t       thread;
    scalar_type  R[17];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 1; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0]  = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1]  = lds[offset_lds + (thread + 1) * lstride]; /* stockham.py:542 */
    R[2]  = lds[offset_lds + (thread + 2) * lstride]; /* stockham.py:542 */
    R[3]  = lds[offset_lds + (thread + 3) * lstride]; /* stockham.py:542 */
    R[4]  = lds[offset_lds + (thread + 4) * lstride]; /* stockham.py:542 */
    R[5]  = lds[offset_lds + (thread + 5) * lstride]; /* stockham.py:542 */
    R[6]  = lds[offset_lds + (thread + 6) * lstride]; /* stockham.py:542 */
    R[7]  = lds[offset_lds + (thread + 7) * lstride]; /* stockham.py:542 */
    R[8]  = lds[offset_lds + (thread + 8) * lstride]; /* stockham.py:542 */
    R[9]  = lds[offset_lds + (thread + 9) * lstride]; /* stockham.py:542 */
    R[10] = lds[offset_lds + (thread + 10) * lstride]; /* stockham.py:542 */
    R[11] = lds[offset_lds + (thread + 11) * lstride]; /* stockham.py:542 */
    R[12] = lds[offset_lds + (thread + 12) * lstride]; /* stockham.py:542 */
    R[13] = lds[offset_lds + (thread + 13) * lstride]; /* stockham.py:542 */
    R[14] = lds[offset_lds + (thread + 14) * lstride]; /* stockham.py:542 */
    R[15] = lds[offset_lds + (thread + 15) * lstride]; /* stockham.py:542 */
    R[16] = lds[offset_lds + (thread + 16) * lstride]; /* stockham.py:542 */

    InvRad17B1(&R[0],
               &R[1],
               &R[2],
               &R[3],
               &R[4],
               &R[5],
               &R[6],
               &R[7],
               &R[8],
               &R[9],
               &R[10],
               &R[11],
               &R[12],
               &R[13],
               &R[14],
               &R[15],
               &R[16]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 8) * lstride]
            = R[8]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 9) * lstride]
            = R[9]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 10) * lstride]
            = R[10]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 11) * lstride]
            = R[11]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 12) * lstride]
            = R[12]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 13) * lstride]
            = R[13]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 14) * lstride]
            = R[14]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 15) * lstride]
            = R[15]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 17 + thread % 1 + 16) * lstride]
            = R[16]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(120) void op_inverse_length17_SBRR(const scalar_type* __restrict__ twiddles,
                                                         const size_t dim,
                                                         const size_t* __restrict__ lengths,
                                                         const size_t* __restrict__ stride_in,
                                                         const size_t* __restrict__ stride_out,
                                                         const size_t       nbatch,
                                                         const unsigned int lds_padding,
                                                         void* __restrict__ load_cb_fn,
                                                         void* __restrict__ load_cb_data,
                                                         uint32_t load_cb_lds_bytes,
                                                         void* __restrict__ store_cb_fn,
                                                         void* __restrict__ store_cb_data,
                                                         scalar_type* __restrict__ buf_in,
                                                         scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf_in, offset_in + (thread + 1) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf_in, offset_in + (thread + 2) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf_in, offset_in + (thread + 3) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf_in, offset_in + (thread + 4) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf_in, offset_in + (thread + 5) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf_in, offset_in + (thread + 6) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf_in, offset_in + (thread + 7) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf_in, offset_in + (thread + 8) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf_in, offset_in + (thread + 9) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf_in, offset_in + (thread + 10) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf_in, offset_in + (thread + 11) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf_in, offset_in + (thread + 12) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf_in, offset_in + (thread + 13) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf_in, offset_in + (thread + 14) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf_in, offset_in + (thread + 15) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf_in, offset_in + (thread + 16) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1] = load_cb(
                buf_in, offset_in + (thread + 16 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 1) * stride0_out,
             lds[offset_lds + thread + 1],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 2) * stride0_out,
             lds[offset_lds + thread + 2],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 3) * stride0_out,
             lds[offset_lds + thread + 3],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 4) * stride0_out,
             lds[offset_lds + thread + 4],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 5) * stride0_out,
             lds[offset_lds + thread + 5],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 6) * stride0_out,
             lds[offset_lds + thread + 6],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 7) * stride0_out,
             lds[offset_lds + thread + 7],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 8) * stride0_out,
             lds[offset_lds + thread + 8],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 9) * stride0_out,
             lds[offset_lds + thread + 9],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 10) * stride0_out,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 11) * stride0_out,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 12) * stride0_out,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 13) * stride0_out,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 14) * stride0_out,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 15) * stride0_out,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 16) * stride0_out,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf_out,
                     offset_out + (thread + 16 + 1) * stride0_out,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_inverse_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    scalar_type* __restrict__ buf_in,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 1]
        = load_cb(buf_in, offset_in + (thread + 1) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 2]
        = load_cb(buf_in, offset_in + (thread + 2) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 3]
        = load_cb(buf_in, offset_in + (thread + 3) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 4]
        = load_cb(buf_in, offset_in + (thread + 4) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 5]
        = load_cb(buf_in, offset_in + (thread + 5) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 6]
        = load_cb(buf_in, offset_in + (thread + 6) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 7]
        = load_cb(buf_in, offset_in + (thread + 7) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 8]
        = load_cb(buf_in, offset_in + (thread + 8) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 9]
        = load_cb(buf_in, offset_in + (thread + 9) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 10]
        = load_cb(buf_in, offset_in + (thread + 10) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 11]
        = load_cb(buf_in, offset_in + (thread + 11) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 12]
        = load_cb(buf_in, offset_in + (thread + 12) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 13]
        = load_cb(buf_in, offset_in + (thread + 13) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 14]
        = load_cb(buf_in, offset_in + (thread + 14) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 15]
        = load_cb(buf_in, offset_in + (thread + 15) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 16]
        = load_cb(buf_in, offset_in + (thread + 16) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1] = load_cb(
                buf_in, offset_in + (thread + 16 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            buf_outre[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_inverse_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 1]
        = {buf_inre[offset_in + (thread + 1) * stride0_in],
           buf_inim[offset_in + (thread + 1) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 2]
        = {buf_inre[offset_in + (thread + 2) * stride0_in],
           buf_inim[offset_in + (thread + 2) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 3]
        = {buf_inre[offset_in + (thread + 3) * stride0_in],
           buf_inim[offset_in + (thread + 3) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 4]
        = {buf_inre[offset_in + (thread + 4) * stride0_in],
           buf_inim[offset_in + (thread + 4) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 5]
        = {buf_inre[offset_in + (thread + 5) * stride0_in],
           buf_inim[offset_in + (thread + 5) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 6]
        = {buf_inre[offset_in + (thread + 6) * stride0_in],
           buf_inim[offset_in + (thread + 6) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 7]
        = {buf_inre[offset_in + (thread + 7) * stride0_in],
           buf_inim[offset_in + (thread + 7) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 8]
        = {buf_inre[offset_in + (thread + 8) * stride0_in],
           buf_inim[offset_in + (thread + 8) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 9]
        = {buf_inre[offset_in + (thread + 9) * stride0_in],
           buf_inim[offset_in + (thread + 9) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {buf_inre[offset_in + (thread + 10) * stride0_in],
           buf_inim[offset_in + (thread + 10) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {buf_inre[offset_in + (thread + 11) * stride0_in],
           buf_inim[offset_in + (thread + 11) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {buf_inre[offset_in + (thread + 12) * stride0_in],
           buf_inim[offset_in + (thread + 12) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {buf_inre[offset_in + (thread + 13) * stride0_in],
           buf_inim[offset_in + (thread + 13) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {buf_inre[offset_in + (thread + 14) * stride0_in],
           buf_inim[offset_in + (thread + 14) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {buf_inre[offset_in + (thread + 15) * stride0_in],
           buf_inim[offset_in + (thread + 15) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {buf_inre[offset_in + (thread + 16) * stride0_in],
           buf_inim[offset_in + (thread + 16) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {buf_inre[offset_in + (thread + 16 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 16 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 1) * stride0_out,
             lds[offset_lds + thread + 1],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 2) * stride0_out,
             lds[offset_lds + thread + 2],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 3) * stride0_out,
             lds[offset_lds + thread + 3],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 4) * stride0_out,
             lds[offset_lds + thread + 4],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 5) * stride0_out,
             lds[offset_lds + thread + 5],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 6) * stride0_out,
             lds[offset_lds + thread + 6],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 7) * stride0_out,
             lds[offset_lds + thread + 7],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 8) * stride0_out,
             lds[offset_lds + thread + 8],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 9) * stride0_out,
             lds[offset_lds + thread + 9],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 10) * stride0_out,
             lds[offset_lds + thread + 10],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 11) * stride0_out,
             lds[offset_lds + thread + 11],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 12) * stride0_out,
             lds[offset_lds + thread + 12],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 13) * stride0_out,
             lds[offset_lds + thread + 13],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 14) * stride0_out,
             lds[offset_lds + thread + 14],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 15) * stride0_out,
             lds[offset_lds + thread + 15],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 16) * stride0_out,
             lds[offset_lds + thread + 16],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            store_cb(buf_out,
                     offset_out + (thread + 16 + 1) * stride0_out,
                     lds[offset_lds + thread + 16 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(120) void op_inverse_length17_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 1 threads per transform
    //   does 120 transforms per thread block
    // therefore it should be called with 120 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 120 + threadIdx.x / 1; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (17 + lds_padding) * (transform % 120); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 1; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 1]
        = {buf_inre[offset_in + (thread + 1) * stride0_in],
           buf_inim[offset_in + (thread + 1) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 2]
        = {buf_inre[offset_in + (thread + 2) * stride0_in],
           buf_inim[offset_in + (thread + 2) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 3]
        = {buf_inre[offset_in + (thread + 3) * stride0_in],
           buf_inim[offset_in + (thread + 3) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 4]
        = {buf_inre[offset_in + (thread + 4) * stride0_in],
           buf_inim[offset_in + (thread + 4) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 5]
        = {buf_inre[offset_in + (thread + 5) * stride0_in],
           buf_inim[offset_in + (thread + 5) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 6]
        = {buf_inre[offset_in + (thread + 6) * stride0_in],
           buf_inim[offset_in + (thread + 6) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 7]
        = {buf_inre[offset_in + (thread + 7) * stride0_in],
           buf_inim[offset_in + (thread + 7) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 8]
        = {buf_inre[offset_in + (thread + 8) * stride0_in],
           buf_inim[offset_in + (thread + 8) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 9]
        = {buf_inre[offset_in + (thread + 9) * stride0_in],
           buf_inim[offset_in + (thread + 9) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 10]
        = {buf_inre[offset_in + (thread + 10) * stride0_in],
           buf_inim[offset_in + (thread + 10) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 11]
        = {buf_inre[offset_in + (thread + 11) * stride0_in],
           buf_inim[offset_in + (thread + 11) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 12]
        = {buf_inre[offset_in + (thread + 12) * stride0_in],
           buf_inim[offset_in + (thread + 12) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 13]
        = {buf_inre[offset_in + (thread + 13) * stride0_in],
           buf_inim[offset_in + (thread + 13) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 14]
        = {buf_inre[offset_in + (thread + 14) * stride0_in],
           buf_inim[offset_in + (thread + 14) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 15]
        = {buf_inre[offset_in + (thread + 15) * stride0_in],
           buf_inim[offset_in + (thread + 15) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 16]
        = {buf_inre[offset_in + (thread + 16) * stride0_in],
           buf_inim[offset_in + (thread + 16) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 0)
        {
            lds[offset_lds + thread + 16 + 1]
                = {buf_inre[offset_in + (thread + 16 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 16 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                            17 - thread % 9 - 0,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                            17 - thread % 9 - 1,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                            17 - thread % 9 - 2,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                            17 - thread % 9 - 3,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                            17 - thread % 9 - 4,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                            17 - thread % 9 - 5,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                            17 - thread % 9 - 6,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                            17 - thread % 9 - 7,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                            17 - thread % 9 - 8,
                                                            9,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[17]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length17_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 0,
                                                             17 - thread % 9 - 0,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 1,
                                                             17 - thread % 9 - 1,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 2,
                                                             17 - thread % 9 - 2,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 3,
                                                             17 - thread % 9 - 3,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 4,
                                                             17 - thread % 9 - 4,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 5,
                                                             17 - thread % 9 - 5,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 6,
                                                             17 - thread % 9 - 6,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 7,
                                                             17 - thread % 9 - 7,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, false>(thread % 9 + 8,
                                                             17 - thread % 9 - 8,
                                                             9,
                                                             &lds[offset_lds],
                                                             0,
                                                             &twiddles[17]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 1) * stride0_out]
        = lds[offset_lds + thread + 1].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 2) * stride0_out]
        = lds[offset_lds + thread + 2].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 3) * stride0_out]
        = lds[offset_lds + thread + 3].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 4) * stride0_out]
        = lds[offset_lds + thread + 4].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 5) * stride0_out]
        = lds[offset_lds + thread + 5].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 6) * stride0_out]
        = lds[offset_lds + thread + 6].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 7) * stride0_out]
        = lds[offset_lds + thread + 7].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 8) * stride0_out]
        = lds[offset_lds + thread + 8].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 9) * stride0_out]
        = lds[offset_lds + thread + 9].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 10) * stride0_out]
        = lds[offset_lds + thread + 10].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 11) * stride0_out]
        = lds[offset_lds + thread + 11].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 12) * stride0_out]
        = lds[offset_lds + thread + 12].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 13) * stride0_out]
        = lds[offset_lds + thread + 13].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 14) * stride0_out]
        = lds[offset_lds + thread + 14].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 15) * stride0_out]
        = lds[offset_lds + thread + 15].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 16) * stride0_out]
        = lds[offset_lds + thread + 16].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 0)
        {
            buf_outre[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 16 + 1) * stride0_out]
                = lds[offset_lds + thread + 16 + 1].y; /* generator.py:908 */
        }
    }
}
POWX_SMALL_GENERATOR(rocfft_internal_dfn_dp_ci_ci_stoc_17,
                     ip_forward_length17_SBRR,
                     ip_inverse_length17_SBRR,
                     op_forward_length17_SBRR,
                     op_inverse_length17_SBRR,
                     double2);
POWX_SMALL_GENERATOR(rocfft_internal_dfn_sp_ci_ci_stoc_17,
                     ip_forward_length17_SBRR,
                     ip_inverse_length17_SBRR,
                     op_forward_length17_SBRR,
                     op_inverse_length17_SBRR,
                     float2);