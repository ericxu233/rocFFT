/*
 * Stockham kernels generated by:
 * 
 *     /home/ugrad/ugrad003/roctosycl/rocFFT/library/src/device/kernel-generator.py --pattern=all --precision=all --groups=150 --manual-small= --manual-large= generate /home/ugrad/ugrad003/roctosycl/rocFFT/builddir/library/src/device/generator/rocfft-kernel-generator
 * 
 * Generator is: /home/ugrad/ugrad003/roctosycl/rocFFT/library/src/device/kernel-generator.py
 * Kernel is: namespace(factors=[8, 3, 3, 3, 3], flavour='uwide', length=648, scheme='CS_KERNEL_STOCKHAM', threads_per_block=256)
 */

#include "kernel_launch.h"
#include "real2complex.h"
#include "rocfft_butterfly_template.h"
#include <hip/hip_runtime.h>

/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void ip_forward_length648_SBRR_device(scalar_type* __restrict__ lds,
                                                 const scalar_type* __restrict__ twiddles,
                                                 size_t       stride_lds,
                                                 unsigned int offset_lds,
                                                 bool         write)
{
    size_t       thread;
    scalar_type  R[8];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 216; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 81) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 162) * lstride]; /* stockham.py:542 */
    R[3] = lds[offset_lds + (thread + 243) * lstride]; /* stockham.py:542 */
    R[4] = lds[offset_lds + (thread + 324) * lstride]; /* stockham.py:542 */
    R[5] = lds[offset_lds + (thread + 405) * lstride]; /* stockham.py:542 */
    R[6] = lds[offset_lds + (thread + 486) * lstride]; /* stockham.py:542 */
    R[7] = lds[offset_lds + (thread + 567) * lstride]; /* stockham.py:542 */

    FwdRad8B1(&R[0], &R[1], &R[2], &R[3], &R[4], &R[5], &R[6], &R[7]); /* stockham.py:566 */

    __syncthreads();
    if(write && thread < 81)
    {
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
    }

    // pass 1
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[7 + 2 * (thread % 8)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[8 + 2 * (thread % 8)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 8) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 16) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 2
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[23 + 2 * (thread % 24)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[24 + 2 * (thread % 24)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 24) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 48) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 3
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[71 + 2 * (thread % 72)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[72 + 2 * (thread % 72)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 72) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 144) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 4
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[215 + 2 * (thread % 216)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[216 + 2 * (thread % 216)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 216) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 432) * lstride]
            = R[2]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(216) void ip_forward_length648_SBRR(const scalar_type* __restrict__ twiddles,
                                                          const size_t dim,
                                                          const size_t* __restrict__ lengths,
                                                          const size_t* __restrict__ stride,
                                                          const size_t       nbatch,
                                                          const unsigned int lds_padding,
                                                          void* __restrict__ load_cb_fn,
                                                          void* __restrict__ load_cb_data,
                                                          uint32_t load_cb_lds_bytes,
                                                          void* __restrict__ store_cb_fn,
                                                          void* __restrict__ store_cb_data,
                                                          scalar_type* __restrict__ buf)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf, offset + (thread + 0) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf, offset + (thread + 216) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf, offset + (thread + 432) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = load_cb(buf, offset + (thread + 432 + 1) * stride0, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(
        buf, offset + (thread + 0) * stride0, lds[offset_lds + thread + 0], store_cb_data, nullptr);
    store_cb(buf,
             offset + (thread + 216) * stride0,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 432) * stride0,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf,
                     offset + (thread + 432 + 1) * stride0,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void ip_forward_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ bufre,
    real_type_t<scalar_type>* __restrict__ bufim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread                       = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0] = {bufre[offset + (thread + 0) * stride0],
                                    bufim[offset + (thread + 0) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {bufre[offset + (thread + 216) * stride0],
           bufim[offset + (thread + 216) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {bufre[offset + (thread + 432) * stride0],
           bufim[offset + (thread + 432) * stride0]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {bufre[offset + (thread + 432 + 1) * stride0],
                   bufim[offset + (thread + 432 + 1) * stride0]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    bufre[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    bufim[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    bufre[offset + (thread + 216) * stride0]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    bufim[offset + (thread + 216) * stride0]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    bufre[offset + (thread + 432) * stride0]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    bufim[offset + (thread + 432) * stride0]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            bufre[offset + (thread + 432 + 1) * stride0]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            bufim[offset + (thread + 432 + 1) * stride0]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void op_forward_length648_SBRR_device(scalar_type* __restrict__ lds,
                                                 const scalar_type* __restrict__ twiddles,
                                                 size_t       stride_lds,
                                                 unsigned int offset_lds,
                                                 bool         write)
{
    size_t       thread;
    scalar_type  R[8];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 216; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 81) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 162) * lstride]; /* stockham.py:542 */
    R[3] = lds[offset_lds + (thread + 243) * lstride]; /* stockham.py:542 */
    R[4] = lds[offset_lds + (thread + 324) * lstride]; /* stockham.py:542 */
    R[5] = lds[offset_lds + (thread + 405) * lstride]; /* stockham.py:542 */
    R[6] = lds[offset_lds + (thread + 486) * lstride]; /* stockham.py:542 */
    R[7] = lds[offset_lds + (thread + 567) * lstride]; /* stockham.py:542 */

    FwdRad8B1(&R[0], &R[1], &R[2], &R[3], &R[4], &R[5], &R[6], &R[7]); /* stockham.py:566 */

    __syncthreads();
    if(write && thread < 81)
    {
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
    }

    // pass 1
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[7 + 2 * (thread % 8)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[8 + 2 * (thread % 8)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 8) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 16) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 2
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[23 + 2 * (thread % 24)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[24 + 2 * (thread % 24)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 24) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 48) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 3
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[71 + 2 * (thread % 72)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[72 + 2 * (thread % 72)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 72) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 144) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 4
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = twiddles[215 + 2 * (thread % 216)]; /* stockham.py:554 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = twiddles[216 + 2 * (thread % 216)]; /* stockham.py:554 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    FwdRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 216) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 432) * lstride]
            = R[2]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(216) void op_forward_length648_SBRR(const scalar_type* __restrict__ twiddles,
                                                          const size_t dim,
                                                          const size_t* __restrict__ lengths,
                                                          const size_t* __restrict__ stride_in,
                                                          const size_t* __restrict__ stride_out,
                                                          const size_t       nbatch,
                                                          const unsigned int lds_padding,
                                                          void* __restrict__ load_cb_fn,
                                                          void* __restrict__ load_cb_data,
                                                          uint32_t load_cb_lds_bytes,
                                                          void* __restrict__ store_cb_fn,
                                                          void* __restrict__ store_cb_data,
                                                          scalar_type* __restrict__ buf_in,
                                                          scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf_in, offset_in + (thread + 216) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf_in, offset_in + (thread + 432) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1] = load_cb(
                buf_in, offset_in + (thread + 432 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 216) * stride0_out,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 432) * stride0_out,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf_out,
                     offset_out + (thread + 432 + 1) * stride0_out,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_forward_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    scalar_type* __restrict__ buf_in,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf_in, offset_in + (thread + 216) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf_in, offset_in + (thread + 432) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1] = load_cb(
                buf_in, offset_in + (thread + 432 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            buf_outre[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_forward_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {buf_inre[offset_in + (thread + 216) * stride0_in],
           buf_inim[offset_in + (thread + 216) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {buf_inre[offset_in + (thread + 432) * stride0_in],
           buf_inim[offset_in + (thread + 432) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {buf_inre[offset_in + (thread + 432 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 432 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 216) * stride0_out,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 432) * stride0_out,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf_out,
                     offset_out + (thread + 432 + 1) * stride0_out,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_forward_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {buf_inre[offset_in + (thread + 216) * stride0_in],
           buf_inim[offset_in + (thread + 216) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {buf_inre[offset_in + (thread + 432) * stride0_in],
           buf_inim[offset_in + (thread + 432) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {buf_inre[offset_in + (thread + 432 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 432 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_forward_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            buf_outre[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void ip_inverse_length648_SBRR_device(scalar_type* __restrict__ lds,
                                                 const scalar_type* __restrict__ twiddles,
                                                 size_t       stride_lds,
                                                 unsigned int offset_lds,
                                                 bool         write)
{
    size_t       thread;
    scalar_type  R[8];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 216; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 81) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 162) * lstride]; /* stockham.py:542 */
    R[3] = lds[offset_lds + (thread + 243) * lstride]; /* stockham.py:542 */
    R[4] = lds[offset_lds + (thread + 324) * lstride]; /* stockham.py:542 */
    R[5] = lds[offset_lds + (thread + 405) * lstride]; /* stockham.py:542 */
    R[6] = lds[offset_lds + (thread + 486) * lstride]; /* stockham.py:542 */
    R[7] = lds[offset_lds + (thread + 567) * lstride]; /* stockham.py:542 */

    InvRad8B1(&R[0], &R[1], &R[2], &R[3], &R[4], &R[5], &R[6], &R[7]); /* stockham.py:566 */

    __syncthreads();
    if(write && thread < 81)
    {
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
    }

    // pass 1
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[7 + 2 * (thread % 8)].x,
         -twiddles[7 + 2 * (thread % 8)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[8 + 2 * (thread % 8)].x,
         -twiddles[8 + 2 * (thread % 8)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 8) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 16) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 2
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[23 + 2 * (thread % 24)].x,
         -twiddles[23 + 2 * (thread % 24)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[24 + 2 * (thread % 24)].x,
         -twiddles[24 + 2 * (thread % 24)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 24) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 48) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 3
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[71 + 2 * (thread % 72)].x,
         -twiddles[71 + 2 * (thread % 72)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[72 + 2 * (thread % 72)].x,
         -twiddles[72 + 2 * (thread % 72)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 72) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 144) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 4
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[215 + 2 * (thread % 216)].x,
         -twiddles[215 + 2 * (thread % 216)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[216 + 2 * (thread % 216)].x,
         -twiddles[216 + 2 * (thread % 216)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 216) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 432) * lstride]
            = R[2]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(216) void ip_inverse_length648_SBRR(const scalar_type* __restrict__ twiddles,
                                                          const size_t dim,
                                                          const size_t* __restrict__ lengths,
                                                          const size_t* __restrict__ stride,
                                                          const size_t       nbatch,
                                                          const unsigned int lds_padding,
                                                          void* __restrict__ load_cb_fn,
                                                          void* __restrict__ load_cb_data,
                                                          uint32_t load_cb_lds_bytes,
                                                          void* __restrict__ store_cb_fn,
                                                          void* __restrict__ store_cb_data,
                                                          scalar_type* __restrict__ buf)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf, offset + (thread + 0) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf, offset + (thread + 216) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf, offset + (thread + 432) * stride0, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = load_cb(buf, offset + (thread + 432 + 1) * stride0, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(
        buf, offset + (thread + 0) * stride0, lds[offset_lds + thread + 0], store_cb_data, nullptr);
    store_cb(buf,
             offset + (thread + 216) * stride0,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf,
             offset + (thread + 432) * stride0,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf,
                     offset + (thread + 432 + 1) * stride0,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void ip_inverse_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ bufre,
    real_type_t<scalar_type>* __restrict__ bufim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset           = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0  = (sb == SB_UNIT) ? (1) : (stride[0]);
    auto         load_cb  = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset        = offset + index_along_d * stride[d]; /* stockham.py:370 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset     = offset + batch * stride[dim]; /* stockham.py:372 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread                       = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0] = {bufre[offset + (thread + 0) * stride0],
                                    bufim[offset + (thread + 0) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {bufre[offset + (thread + 216) * stride0],
           bufim[offset + (thread + 216) * stride0]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {bufre[offset + (thread + 432) * stride0],
           bufim[offset + (thread + 432) * stride0]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {bufre[offset + (thread + 432 + 1) * stride0],
                   bufim[offset + (thread + 432 + 1) * stride0]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    ip_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    bufre[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    bufim[offset + (thread + 0) * stride0] = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    bufre[offset + (thread + 216) * stride0]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    bufim[offset + (thread + 216) * stride0]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    bufre[offset + (thread + 432) * stride0]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    bufim[offset + (thread + 432) * stride0]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            bufre[offset + (thread + 432 + 1) * stride0]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            bufim[offset + (thread + 432 + 1) * stride0]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:765 */
template <typename scalar_type, StrideBin sb>
__device__ void op_inverse_length648_SBRR_device(scalar_type* __restrict__ lds,
                                                 const scalar_type* __restrict__ twiddles,
                                                 size_t       stride_lds,
                                                 unsigned int offset_lds,
                                                 bool         write)
{
    size_t       thread;
    scalar_type  R[8];
    scalar_type  W;
    scalar_type  t;
    const size_t lstride = (sb == SB_UNIT) ? (1) : (stride_lds);
    thread               = threadIdx.x % 216; /* stockham.py:731 */

    // pass 0
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 81) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 162) * lstride]; /* stockham.py:542 */
    R[3] = lds[offset_lds + (thread + 243) * lstride]; /* stockham.py:542 */
    R[4] = lds[offset_lds + (thread + 324) * lstride]; /* stockham.py:542 */
    R[5] = lds[offset_lds + (thread + 405) * lstride]; /* stockham.py:542 */
    R[6] = lds[offset_lds + (thread + 486) * lstride]; /* stockham.py:542 */
    R[7] = lds[offset_lds + (thread + 567) * lstride]; /* stockham.py:542 */

    InvRad8B1(&R[0], &R[1], &R[2], &R[3], &R[4], &R[5], &R[6], &R[7]); /* stockham.py:566 */

    __syncthreads();
    if(write && thread < 81)
    {
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 1) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 2) * lstride]
            = R[2]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 3) * lstride]
            = R[3]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 4) * lstride]
            = R[4]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 5) * lstride]
            = R[5]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 6) * lstride]
            = R[6]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 1) * 8 + thread % 1 + 7) * lstride]
            = R[7]; /* stockham.py:577 */
    }

    // pass 1
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[7 + 2 * (thread % 8)].x,
         -twiddles[7 + 2 * (thread % 8)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[8 + 2 * (thread % 8)].x,
         -twiddles[8 + 2 * (thread % 8)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 8) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 8) * 24 + thread % 8 + 16) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 2
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[23 + 2 * (thread % 24)].x,
         -twiddles[23 + 2 * (thread % 24)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[24 + 2 * (thread % 24)].x,
         -twiddles[24 + 2 * (thread % 24)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 24) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 24) * 72 + thread % 24 + 48) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 3
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[71 + 2 * (thread % 72)].x,
         -twiddles[71 + 2 * (thread % 72)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[72 + 2 * (thread % 72)].x,
         -twiddles[72 + 2 * (thread % 72)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 72) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 72) * 216 + thread % 72 + 144) * lstride]
            = R[2]; /* stockham.py:577 */
    }

    // pass 4
    __syncthreads();
    R[0] = lds[offset_lds + (thread + 0) * lstride]; /* stockham.py:542 */
    R[1] = lds[offset_lds + (thread + 216) * lstride]; /* stockham.py:542 */
    R[2] = lds[offset_lds + (thread + 432) * lstride]; /* stockham.py:542 */

    W    = {twiddles[215 + 2 * (thread % 216)].x,
         -twiddles[215 + 2 * (thread % 216)].y}; /* generator.py:1065 */
    t.x  = W.x * R[1].x - W.y * R[1].y; /* stockham.py:555 */
    t.y  = W.y * R[1].x + W.x * R[1].y; /* stockham.py:556 */
    R[1] = t; /* stockham.py:557 */
    W    = {twiddles[216 + 2 * (thread % 216)].x,
         -twiddles[216 + 2 * (thread % 216)].y}; /* generator.py:1065 */
    t.x  = W.x * R[2].x - W.y * R[2].y; /* stockham.py:555 */
    t.y  = W.y * R[2].x + W.x * R[2].y; /* stockham.py:556 */
    R[2] = t; /* stockham.py:557 */

    InvRad3B1(&R[0], &R[1], &R[2]); /* stockham.py:566 */

    __syncthreads();
    if(write)
    {
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 0) * lstride]
            = R[0]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 216) * lstride]
            = R[1]; /* stockham.py:577 */
        lds[offset_lds + ((thread / 216) * 648 + thread % 216 + 432) * lstride]
            = R[2]; /* stockham.py:577 */
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__
    __launch_bounds__(216) void op_inverse_length648_SBRR(const scalar_type* __restrict__ twiddles,
                                                          const size_t dim,
                                                          const size_t* __restrict__ lengths,
                                                          const size_t* __restrict__ stride_in,
                                                          const size_t* __restrict__ stride_out,
                                                          const size_t       nbatch,
                                                          const unsigned int lds_padding,
                                                          void* __restrict__ load_cb_fn,
                                                          void* __restrict__ load_cb_data,
                                                          uint32_t load_cb_lds_bytes,
                                                          void* __restrict__ store_cb_fn,
                                                          void* __restrict__ store_cb_data,
                                                          scalar_type* __restrict__ buf_in,
                                                          scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf_in, offset_in + (thread + 216) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf_in, offset_in + (thread + 432) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1] = load_cb(
                buf_in, offset_in + (thread + 432 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 216) * stride0_out,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 432) * stride0_out,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf_out,
                     offset_out + (thread + 432 + 1) * stride0_out,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_inverse_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    scalar_type* __restrict__ buf_in,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = load_cb(buf_in, offset_in + (thread + 0) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 216]
        = load_cb(buf_in, offset_in + (thread + 216) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */
    lds[offset_lds + thread + 432]
        = load_cb(buf_in, offset_in + (thread + 432) * stride0_in, load_cb_data, nullptr);
    ; /* stockham.py:387 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1] = load_cb(
                buf_in, offset_in + (thread + 432 + 1) * stride0_in, load_cb_data, nullptr);
            ; /* stockham.py:395 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            buf_outre[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_inverse_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    scalar_type* __restrict__ buf_out)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {buf_inre[offset_in + (thread + 216) * stride0_in],
           buf_inim[offset_in + (thread + 216) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {buf_inre[offset_in + (thread + 432) * stride0_in],
           buf_inim[offset_in + (thread + 432) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {buf_inre[offset_in + (thread + 432 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 432 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    store_cb(buf_out,
             offset_out + (thread + 0) * stride0_out,
             lds[offset_lds + thread + 0],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 216) * stride0_out,
             lds[offset_lds + thread + 216],
             store_cb_data,
             nullptr);
    store_cb(buf_out,
             offset_out + (thread + 432) * stride0_out,
             lds[offset_lds + thread + 432],
             store_cb_data,
             nullptr);

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            store_cb(buf_out,
                     offset_out + (thread + 432 + 1) * stride0_out,
                     lds[offset_lds + thread + 432 + 1],
                     store_cb_data,
                     nullptr);
        }
    }
}
/* stockham.py:709 */
template <typename scalar_type, StrideBin sb, EmbeddedType ebtype, CallbackType cbtype>
__global__ __launch_bounds__(216) void op_inverse_length648_SBRR(
    const scalar_type* __restrict__ twiddles,
    const size_t dim,
    const size_t* __restrict__ lengths,
    const size_t* __restrict__ stride_in,
    const size_t* __restrict__ stride_out,
    const size_t       nbatch,
    const unsigned int lds_padding,
    void* __restrict__ load_cb_fn,
    void* __restrict__ load_cb_data,
    uint32_t load_cb_lds_bytes,
    void* __restrict__ store_cb_fn,
    void* __restrict__ store_cb_data,
    real_type_t<scalar_type>* __restrict__ buf_inre,
    real_type_t<scalar_type>* __restrict__ buf_inim,
    real_type_t<scalar_type>* __restrict__ buf_outre,
    real_type_t<scalar_type>* __restrict__ buf_outim)
{
    // this kernel:
    //   uses 216 threads per transform
    //   does 1 transforms per thread block
    // therefore it should be called with 216 threads per thread block
    extern __shared__ unsigned char __align__(sizeof(scalar_type)) lds_uchar[];
    scalar_type* __restrict__ lds = reinterpret_cast<scalar_type*>(lds_uchar);
    size_t       offset_in        = 0;
    size_t       offset_out       = 0;
    unsigned int offset_lds;
    size_t       stride_lds;
    size_t       batch;
    size_t       transform;
    size_t       thread;
    bool         write;
    const size_t stride0_in  = (sb == SB_UNIT) ? (1) : (stride_in[0]);
    const size_t stride0_out = (sb == SB_UNIT) ? (1) : (stride_out[0]);
    auto         load_cb     = get_load_cb<scalar_type, cbtype>(load_cb_fn);
    auto         store_cb    = get_store_cb<scalar_type, cbtype>(store_cb_fn);

    // large twiddles

    // offsets
    size_t remaining;
    size_t plength = 1;
    size_t index_along_d;
    transform = blockIdx.x * 1 + threadIdx.x / 216; /* stockham.py:363 */
    remaining = transform; /* stockham.py:364 */
    for(int d = 1; d < dim; ++d)
    {
        plength       = plength * lengths[d]; /* stockham.py:367 */
        index_along_d = remaining % lengths[d]; /* stockham.py:368 */
        remaining     = remaining / lengths[d]; /* stockham.py:369 */
        offset_in     = offset_in + index_along_d * stride_in[d]; /* generator.py:992 */
        offset_out    = offset_out + index_along_d * stride_out[d]; /* generator.py:993 */
    }
    batch      = transform / plength; /* stockham.py:371 */
    offset_in  = offset_in + batch * stride_in[dim]; /* generator.py:992 */
    offset_out = offset_out + batch * stride_out[dim]; /* generator.py:993 */
    offset_lds = (648 + lds_padding) * (transform % 1); /* stockham.py:373 */

    if(batch >= nbatch)
    {
        return;
    }

    // load global
    thread = threadIdx.x % 216; /* stockham.py:384 */
    lds[offset_lds + thread + 0]
        = {buf_inre[offset_in + (thread + 0) * stride0_in],
           buf_inim[offset_in + (thread + 0) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 216]
        = {buf_inre[offset_in + (thread + 216) * stride0_in],
           buf_inim[offset_in + (thread + 216) * stride0_in]}; /* generator.py:872 */
    lds[offset_lds + thread + 432]
        = {buf_inre[offset_in + (thread + 432) * stride0_in],
           buf_inim[offset_in + (thread + 432) * stride0_in]}; /* generator.py:872 */

    // append extra global loading for C2Real pre-process only
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        // use the last thread of each transform to load one more element per row
        if(thread == 215)
        {
            lds[offset_lds + thread + 432 + 1]
                = {buf_inre[offset_in + (thread + 432 + 1) * stride0_in],
                   buf_inim[offset_in + (thread + 432 + 1) * stride0_in]}; /* generator.py:872 */
        }
    }

    // handle even-length real to complex pre-process in lds before transform
    if(ebtype == EmbeddedType::C2Real_PRE)
    {
        __syncthreads();

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                           648 - thread % 324 - 0,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        real_pre_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                           648 - thread % 324 - 216,
                                                           324,
                                                           &lds[offset_lds],
                                                           0,
                                                           &twiddles[648]); /* stockham.py:338 */

        __syncthreads();
    }

    // transform
    write = true; /* stockham.py:681 */
    op_inverse_length648_SBRR_device<scalar_type, SB_UNIT>(
        lds, twiddles, stride_lds, offset_lds, write); /* stockham.py:686 */

    // handle even-length complex to real post-process in lds after transform
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        __syncthreads();

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 0,
                                                            648 - thread % 324 - 0,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */

        real_post_process_kernel_inplace<scalar_type, true>(thread % 324 + 216,
                                                            648 - thread % 324 - 216,
                                                            324,
                                                            &lds[offset_lds],
                                                            0,
                                                            &twiddles[648]); /* stockham.py:338 */
    }

    // store global
    __syncthreads();
    buf_outre[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 0) * stride0_out]
        = lds[offset_lds + thread + 0].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 216) * stride0_out]
        = lds[offset_lds + thread + 216].y; /* generator.py:908 */
    buf_outre[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].x; /* generator.py:906 */
    buf_outim[offset_out + (thread + 432) * stride0_out]
        = lds[offset_lds + thread + 432].y; /* generator.py:908 */

    // append extra global write for Real2C post-process only
    if(ebtype == EmbeddedType::Real2C_POST)
    {
        // use the last thread of each transform to write one more element per row
        if(thread == 215)
        {
            buf_outre[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].x; /* generator.py:906 */
            buf_outim[offset_out + (thread + 432 + 1) * stride0_out]
                = lds[offset_lds + thread + 432 + 1].y; /* generator.py:908 */
        }
    }
}
POWX_SMALL_GENERATOR(rocfft_internal_dfn_dp_ci_ci_stoc_648,
                     ip_forward_length648_SBRR,
                     ip_inverse_length648_SBRR,
                     op_forward_length648_SBRR,
                     op_inverse_length648_SBRR,
                     double2);
POWX_SMALL_GENERATOR(rocfft_internal_dfn_sp_ci_ci_stoc_648,
                     ip_forward_length648_SBRR,
                     ip_inverse_length648_SBRR,
                     op_forward_length648_SBRR,
                     op_inverse_length648_SBRR,
                     float2);